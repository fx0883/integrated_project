---
description:
globs:
alwaysApply: false
---
# API文档摘要

## API设计原则

系统API遵循RESTful设计原则，主要特点包括：

1. **统一的URL结构**：使用资源名称的复数形式作为URL路径
2. **HTTP方法语义**：使用GET、POST、PUT、DELETE表示不同操作
3. **状态码使用**：使用标准HTTP状态码表示操作结果
4. **分页机制**：支持基于偏移量和基于游标的分页
5. **过滤和排序**：支持通过查询参数进行过滤和排序
6. **版本控制**：API路径包含版本号（如/api/v1/）

## 统一响应格式

所有API响应都遵循以下统一格式：

```json
{
  "success": true/false,
  "code": 2000,
  "message": "操作成功/失败信息",
  "data": { ... }
}
```

- `success`: 布尔值，表示操作是否成功
- `code`: 整数，表示业务状态码
- `message`: 字符串，表示操作结果的描述信息
- `data`: 对象或数组，表示响应数据

## 核心API概览

### 认证API

| 路径 | 方法 | 描述 | 权限 |
|------|------|------|------|
| `/api/v1/auth/login/` | POST | 用户登录，获取JWT令牌 | 无需认证 |
| `/api/v1/auth/refresh/` | POST | 刷新JWT令牌 | 无需认证 |
| `/api/v1/auth/verify/` | POST | 验证JWT令牌有效性 | 无需认证 |
| `/api/v1/auth/password-reset-request/` | POST | 请求密码重置 | 无需认证 |
| `/api/v1/auth/password-reset/` | POST | 重置密码 | 无需认证 |

### 用户管理API

| 路径 | 方法 | 描述 | 权限 |
|------|------|------|------|
| `/api/v1/admin/users/` | GET | 获取用户列表 | 管理员 |
| `/api/v1/admin/users/` | POST | 创建新用户 | 管理员 |
| `/api/v1/admin/users/{id}/` | GET | 获取用户详情 | 管理员 |
| `/api/v1/admin/users/{id}/` | PUT | 更新用户信息 | 管理员 |
| `/api/v1/admin/users/{id}/` | DELETE | 删除用户 | 管理员 |
| `/api/v1/users/me/` | GET | 获取当前用户信息 | 已认证用户 |
| `/api/v1/users/me/` | PUT | 更新当前用户信息 | 已认证用户 |
| `/api/v1/users/me/password/` | PUT | 修改当前用户密码 | 已认证用户 |

### 租户管理API

| 路径 | 方法 | 描述 | 权限 |
|------|------|------|------|
| `/api/v1/tenants/` | GET | 获取租户列表 | 超级管理员 |
| `/api/v1/tenants/` | POST | 创建新租户 | 超级管理员 |
| `/api/v1/tenants/{id}/` | GET | 获取租户详情 | 超级管理员/租户管理员 |
| `/api/v1/tenants/{id}/` | PUT | 更新租户信息 | 超级管理员 |
| `/api/v1/tenants/{id}/` | DELETE | 删除租户 | 超级管理员 |
| `/api/v1/tenants/{id}/quota/` | GET | 获取租户配额信息 | 超级管理员/租户管理员 |
| `/api/v1/tenants/{id}/quota/` | PUT | 更新租户配额 | 超级管理员 |
| `/api/v1/tenants/{id}/activate/` | POST | 激活租户 | 超级管理员 |
| `/api/v1/tenants/{id}/suspend/` | POST | 暂停租户 | 超级管理员 |

### 权限管理API

| 路径 | 方法 | 描述 | 权限 |
|------|------|------|------|
| `/api/v1/rbac/permissions/` | GET | 获取权限列表 | 管理员 |
| `/api/v1/rbac/permissions/` | POST | 创建新权限 | 超级管理员 |
| `/api/v1/rbac/roles/` | GET | 获取角色列表 | 管理员 |
| `/api/v1/rbac/roles/` | POST | 创建新角色 | 管理员 |
| `/api/v1/rbac/roles/{id}/` | GET | 获取角色详情 | 管理员 |
| `/api/v1/rbac/roles/{id}/` | PUT | 更新角色 | 管理员 |
| `/api/v1/rbac/roles/{id}/permissions/` | GET | 获取角色的权限 | 管理员 |
| `/api/v1/rbac/roles/{id}/permissions/` | PUT | 更新角色的权限 | 管理员 |
| `/api/v1/rbac/users/{user_id}/roles/` | GET | 获取用户的角色 | 管理员 |
| `/api/v1/rbac/users/{user_id}/roles/` | POST | 为用户分配角色 | 管理员 |
| `/api/v1/rbac/check-permission/` | GET | 检查权限 | 已认证用户 |

### 菜单管理API

| 路径 | 方法 | 描述 | 权限 |
|------|------|------|------|
| `/api/v1/menus/` | GET | 获取菜单列表 | 管理员 |
| `/api/v1/menus/` | POST | 创建新菜单 | 管理员 |
| `/api/v1/menus/{id}/` | GET | 获取菜单详情 | 管理员 |
| `/api/v1/menus/{id}/` | PUT | 更新菜单 | 管理员 |
| `/api/v1/menus/{id}/` | DELETE | 删除菜单 | 管理员 |
| `/api/v1/menus/tree/` | GET | 获取菜单树结构 | 管理员 |
| `/api/v1/users/me/menus/` | GET | 获取当前用户的菜单 | 已认证用户 |
| `/api/v1/admin/menus/routes/` | GET | 获取前端路由配置 | 管理员 |

### CMS内容管理API

| 路径 | 方法 | 描述 | 权限 |
|------|------|------|------|
| `/api/v1/cms/articles/` | GET | 获取文章列表 | 视情况而定 |
| `/api/v1/cms/articles/` | POST | 创建新文章 | 内容管理员 |
| `/api/v1/cms/articles/{id}/` | GET | 获取文章详情 | 视情况而定 |
| `/api/v1/cms/articles/{id}/` | PUT | 更新文章 | 内容管理员 |
| `/api/v1/cms/articles/{id}/` | DELETE | 删除文章 | 内容管理员 |
| `/api/v1/cms/categories/` | GET | 获取分类列表 | 视情况而定 |
| `/api/v1/cms/comments/` | GET | 获取评论列表 | 视情况而定 |
| `/api/v1/cms/comments/` | POST | 创建新评论 | 已认证用户 |

### 打卡系统API

| 路径 | 方法 | 描述 | 权限 |
|------|------|------|------|
| `/api/v1/check-system/check-in/` | POST | 用户签到打卡 | 已认证用户 |
| `/api/v1/check-system/check-records/` | GET | 获取打卡记录 | 已认证用户 |
| `/api/v1/check-system/statistics/` | GET | 获取打卡统计 | 已认证用户 |

### 图表数据API

| 路径 | 方法 | 描述 | 权限 |
|------|------|------|------|
| `/api/v1/admin/charts/tenant-overview/` | GET | 获取租户概览图表数据 | 管理员 |
| `/api/v1/admin/charts/user-growth/` | GET | 获取用户增长趋势图表数据 | 管理员 |
| `/api/v1/admin/charts/resource-usage/` | GET | 获取资源使用情况图表数据 | 管理员 |

## API认证机制

系统使用JWT（JSON Web Token）进行API认证：

1. 用户通过`/api/v1/auth/login/`获取JWT令牌
2. 后续请求在Authorization头中携带令牌：`Authorization: Bearer <token>`
3. 令牌包含用户ID、租户ID等信息，服务器通过验证令牌确认用户身份
4. 令牌有效期为24小时，可以通过`/api/v1/auth/refresh/`刷新

## API权限控制

API权限控制基于以下机制：

1. **基于角色的权限控制**：用户根据其角色获得不同的权限
2. **租户隔离**：用户只能访问其所属租户的数据
3. **超级管理员特权**：超级管理员可以访问所有租户的数据
4. **对象级权限**：某些API支持对象级别的权限控制

## 常见错误码

| 错误码 | 描述 |
|--------|------|
| 2000 | 成功 |
| 4000 | 一般错误 |
| 4001 | 认证失败 |
| 4002 | 权限不足 |
| 4003 | 参数错误 |
| 4004 | 资源不存在 |
| 4005 | 资源已存在 |
| 4006 | 配额超限 |
| 5000 | 服务器内部错误 |
