---
description:
globs:
alwaysApply: false
---
# 租户模块 (tenants)

## 功能概述

租户模块是多租户系统的基础，负责管理系统中的租户实体，实现租户的创建、配置、状态管理和资源配额控制。该模块使系统能够在同一实例上为多个组织提供服务，同时确保数据隔离和资源限制。

## 核心组件

- **租户管理**：创建、查询、更新和删除租户
- **租户状态控制**：管理租户的活跃、暂停和删除状态
- **资源配额管理**：控制每个租户可使用的用户数、管理员数、存储空间等资源
- **租户隔离**：确保不同租户之间的数据隔离
- **租户与客户关系管理**：管理租户与客户之间的关联

## 数据模型

### 租户模型 (Tenant)
- 主要字段：
  - `name`: 租户名称
  - `code`: 租户代码（用于API和集成的唯一标识符）
  - `status`: 租户状态（活跃、暂停、已删除）
  - `contact_name`: 联系人姓名
  - `contact_email`: 联系人邮箱
  - `contact_phone`: 联系人电话
  - `is_deleted`: 是否已删除（软删除标记）

### 租户配额模型 (TenantQuota)
- 与租户一对一关联
- 主要字段：
  - `tenant`: 关联到租户
  - `max_users`: 最大用户数
  - `max_admins`: 最大管理员数
  - `max_storage_mb`: 最大存储空间(MB)
  - `max_products`: 最大产品数
  - `current_storage_used_mb`: 当前已用存储空间(MB)

## 与其他模块的关系

1. **与用户模块 (users) 的关系**:
   - 用户模型通过外键关联到租户
   - 租户负责控制用户数量的配额
   - 租户状态影响关联用户的可用性

2. **与权限模块 (rbac) 的关系**:
   - 角色可以关联到特定租户
   - 租户特定的权限控制

3. **与客户模块 (customers) 的关系**:
   - 通过CustomerTenantRelation关联租户和客户
   - 支持多种关系类型（如客户、供应商等）

4. **与通用模块 (common) 的关系**:
   - 租户中间件提供租户上下文
   - API日志记录租户信息

## 关键API

### 租户管理API
- `GET /api/v1/tenants/`: 获取租户列表
- `POST /api/v1/tenants/`: 创建新租户
- `GET /api/v1/tenants/{id}/`: 获取租户详情
- `PUT /api/v1/tenants/{id}/`: 更新租户信息
- `DELETE /api/v1/tenants/{id}/`: 删除租户（软删除）

### 租户配额API
- `GET /api/v1/tenants/{id}/quota/`: 获取租户配额信息
- `PUT /api/v1/tenants/{id}/quota/`: 更新租户配额
- `GET /api/v1/tenants/{id}/quota/usage/`: 获取租户资源使用情况

### 租户状态管理API
- `POST /api/v1/tenants/{id}/activate/`: 激活租户
- `POST /api/v1/tenants/{id}/suspend/`: 暂停租户
- `POST /api/v1/tenants/{id}/restore/`: 恢复已删除的租户

## 业务逻辑

1. **租户创建逻辑**:
   - 验证租户名称唯一性
   - 自动生成租户代码（如果未提供）
   - 创建默认配额记录

2. **租户状态管理**:
   - 活跃状态：租户可以正常使用系统
   - 暂停状态：租户暂时无法使用系统，但数据保留
   - 删除状态：租户被标记为删除，但数据仍然保留（软删除）

3. **资源配额控制**:
   - 限制租户可创建的用户数量
   - 限制租户可分配的管理员数量
   - 限制租户可使用的存储空间
   - 提供配额使用情况的统计和监控

4. **租户隔离机制**:
   - 通过TenantMiddleware实现请求级别的租户上下文
   - 确保用户只能访问其所属租户的数据
   - 超级管理员可以跨租户访问数据

5. **租户与客户关系**:
   - 支持多种关系类型
   - 关系可以设置有效期
   - 提供活跃关系的查询方法
